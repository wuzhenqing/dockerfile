# Hadolint configuration file
# See https://github.com/hadolint/hadolint for more information

# Ignore specific rules that may not apply to our Dockerfiles
ignored:
  # DL3008: Pin versions in apt get install
  # Some packages may not have specific versions available
  - DL3008
  # DL3009: Delete the apt-get lists after installing something
  # We use apt-get clean/autoclean instead
  - DL3009
  # DL3015: Avoid additional packages by using `--no-install-recommends`
  # We intentionally install recommended packages for development environments
  - DL3015
  # DL3059: Multiple consecutive `RUN` instructions
  # We combine RUN instructions for better layer caching
  - DL3059
  # SC2086: Double quote to prevent globbing and word splitting
  # Some shell variables are intentionally unquoted
  - SC2086
  # SC2039: In POSIX sh, something is undefined
  # We use bash-specific features intentionally
  - SC2039
  # DL4006: Set the `SHELL` option -o pipefail before `RUN` with a pipe in
  # We handle errors explicitly in our scripts
  - DL4006

